# RESTART PROMPT (paste this + the full file below at the start of a new session)

Cat or paste the **entire contents** of `prompts/resets/2026-06-09_full_context_reset.md` immediately after this line for the complete reset.

---

(Full reset prompt content follows in the referenced file. It forces reading all context docs first with read-only tools only, then strict discussion-only mode until the user explicitly authorizes action. It includes the full history, current state, and the critical "do not start writing code" rule based on past violations.)

To use: In a new conversation, start with this file's content followed by the full `prompts/resets/2026-06-09_full_context_reset.md`.