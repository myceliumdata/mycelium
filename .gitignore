.venv/
.env
__pycache__/
*.py[cod]
*.egg-info/
src/mycelium.egg-info/
dist/
build/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
.langgraph_api/

# Retired legacy prototype shim — ignore leftover local runtime artifacts
data/

# Framework src/agents/specialists/*_specialist.py are committed (CI + import_module fallback).
# Regenerate from specialist_agent.py.j2 when the template changes.

# Example networks: runtime artifacts must not exist under examples/networks/ — enforced by
# tests/test_example_network.py (no gitignore; stray files stay visible in git status).

# User scratch/dump area for files to share with the AI (e.g. logs, outputs, traces)
tmp/

admin-ui/node_modules/
admin-ui/dist/
.env.~*~
docs/.DS_Store
docs/manual-checks/runs/
