# agent-factory-07-polish-refine-verify — Output

## Claim

Moved `prompts/cursor/next/2026-06-07-agent-factory-07-polish-refine-verify.md` → `in-progress/.../prompt.md` before implementation.

## Summary

Final Phase 2 slice:

1. **`_refine_with_llm`** — lazy `ChatOpenAI`, style-review prompt, temperature 0, guard requires `agent_name` and `def {agent_name}` in output else returns original
2. **`test_agent_factory_refine_with_mock_llm`** — mocks `langchain_openai.ChatOpenAI`; exercises improve path + guard fallback (no API key)
3. **`docs/architecture.md`** — Phase 2 Agent Factory paragraph under Derivative / Non-Core Data
4. **Full verification matrix** executed (below)

**Phase 2 Agent Factory per approved plan is complete** (slices 01–07).

## Code changes

| File | Change |
|------|--------|
| `src/agents/factory/agent_factory.py` | Real `_refine_with_llm` body |
| `tests/test_agent_factory.py` | `test_agent_factory_refine_with_mock_llm` only |
| `docs/architecture.md` | Phase 2 paragraph (+ minor surrounding context alignment) |

### Test file (Guard rule)

```
git diff --stat tests/test_agent_factory.py
(~45 lines added: test_agent_factory_refine_with_mock_llm only)
```

Test changes strictly limited to the described refine smoke test + guard fallback assertion. No unrelated restorations.

## Verification

### Automated

```
$ uv run pytest -m smoke -q
28 passed, 9 deselected

$ uv run pytest -m full -q -k "non_core or query_non_core or supervisor or graph or factory or registry or agent"
7 passed, 30 deselected

$ uv run ruff check src/agents/factory/agent_factory.py tests/test_agent_factory.py docs/architecture.md
All checks passed!
```

### Manual matrix

**Core query:** `Found core record for Nichanan Kesonpat.`

**Non-core `net_worth` (first run):**
- Message: `researching net_worth (via financial_specialist).`
- Git commit: `711c36f feat(agents): auto-generate financial_specialist for category 'financial'`
- Header: `AUTO-GENERATED by Agent Factory on 2026-06-03T17:08:19...`
- `data/agents/financial/storage.json` + `storage_strategy.json` present

**Second `net_worth`:** no new create; same `(via financial_specialist)` message.

**Registry reload:**
```
callable: True
has response: True
```

**Isolation (`/tmp` envs, `auto_commit=False`):**
```
created: True, path: /tmp/s-iso/hobby_specialist.py, committed: False
src polluted: False
```

**Delete registry reseed:** `has core_data: True` after remove + reset (embedded seed).

**Header grep:** `financial_specialist.py`, `demographic_specialist.py` under `src/agents/specialists/`.

**No hot-path LLM in factory except refine:** `ChatOpenAI` only in `agent_factory.py` `_refine_with_llm` and `classification/engine.py` (classify unknowns + refresh).

**MCP registry list:** `core_data`, `demographic_specialist`, `financial_specialist` (3 agents).

## Success criteria (approved plan)

| Criterion | Met |
|-----------|-----|
| On-demand specialist creation + registry + dispatch | Yes |
| AUTO-GENERATED header in committed `.py` | Yes |
| Git commit on real creation | Yes (`711c36f`) |
| Per-category storage json sidecars | Yes |
| Supervisor thin (trigger only) | Yes |
| `specialist=` / `(via …)` observability | Yes (slice 06) |
| Tests isolated (tmp envs, no pytest commits) | Yes |
| Optional LLM refine (off by default, mock-tested) | Yes |
| Smoke + full targeted green | Yes |

## Re-run note

Verification commands from the approved plan were re-run for this final slice per "After Cursor delivers each slice."

## Follow-ups (out of scope)

- Pre-generate remaining taxonomy specialists
- Publish `docs/plans/agent-factory-phase2-plan.md` snapshot if desired
- Align stale supervisor section bullets in `architecture.md` (lines 62–69 still mention Phase-1-only routing in places)
