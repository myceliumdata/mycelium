# Slice 1600 — Integration, tests, docs, specialist re-gen (reprocess)

## Claim

Moved `prompts/cursor/next/2026-06-09-1600-integration-tests-reset-docs-regen-reprocess.md` → `prompts/cursor/in-progress/.../prompt.md`, then delivered here.

## Summary

Capstone integration for the seed-data-context redesign: verified `bin/reset-mycelium` (no `core_data`, no `seed.json` handling), updated MCP/CLI/docs, re-generated all six specialists from the 1540 Jinja template, improved trace-capture test isolation, and verified smoke/full/manual paths.

## Specialist re-generation

After `./bin/reset-mycelium --specialists --yes --no-git`, ran `AgentFactory.create_specialist` for all categories in `data/categories.json` plus `financial` (`auto_commit=False`).

Verified `contact_specialist.py`:
- `AUTO-GENERATED by Agent Factory` header
- No `core_identity` import
- 3-scenario docstring + `specialist_contrib` return shape
- TODO on stuck `pending`
- Context / `person_id` / owned-fields handling

Registry: `data/agent_registry.json` lists six generated specialists (no `core_data`).

## reset-mycelium

- No `CORE_AGENT_NAME` (removed in 1530)
- `--base` message references `data/seed.json` via `agents.seed`, not CRM reseed wording
- No references to touching `data/seed.json`
- Dry-run: `./bin/reset-mycelium --dry-run --base --specialists` — OK

## Docs / CLI / MCP

- `docs/architecture.md` — seed origin, graph flow, no core specialist
- `docs/plans/seed-data-context-architecture.md` — implemented banner + slice table
- `TODO.md` — redesign landed note
- `src/main.py` — specialist-status help; legacy `seed` subcommand description
- `src/mycelium_mcp/server.py` — default `MYCELIUM_SEED_PATH=data/seed.json`; message wording

## Tests

`tests/test_trace_capture.py` — clears `MYCELIUM_USE_SYNC_CHECKPOINTER`, resets graph/registry/seed singletons, mocks `_invoke_core_graph` / `_invoke_sync_graph` to set `_last_invocation_trace_id` after real graph invoke.

## Verification

```text
$ uv run ruff check src/ tests/ bin/reset-mycelium
All checks passed!

$ uv run pytest -m smoke -q
23 passed, 11 deselected in 0.68s

$ uv run pytest -m full -q
9 passed, 25 deselected in 0.24s

$ grep core_data src/  # comments only in legacy modules
src/agents/classification/...
src/agents/core_identity.py
src/agents/responses.py
src/agents/person_prep.py

$ uv run python -m main query --person-key person-0001
message: Found record for Nichanan Kesonpat.

$ uv run python -m main query --person-key person-0001 --attributes email
message: ... email not currently available but may be in the future (via contact_specialist).
debug: outcome='assembled'; contributions=1
```

## Scope confirmation

Integration capstone only (no 1700 UUID-in-results or 1710/1720 follow-ups).

**Ready for next slice:** `2026-06-09-1700-expose-uuid-in-results-reprocess.md`
