# Task: Agent Factory Phase 2 - Slice 04: Agent Factory (render + write + storage init + registry update + dynamic load + git + stub refine) + dedicated factory tests

**Read these first (mandatory):**
- `docs/plans/agent-factory-phase2.md` (the approved plan - focus on Agent Factory component, "Jinja2 template rendering + optional LLM refinement", "File generation + automatic git commit", Design Decisions, Generated Agent Example)
- The detailed implementation plan at `docs/plans/agent-factory-phase2.md` (focus on "### Jinja2 Templates" full .j2 , "Agent Factory Implementation Notes", Step 4, risks "Generated code quality" "Git clutter" "Test isolation", "Proposed Final File/Folder Structure", and Step 4 verification commands)
- `prompts/cursor/WORKFLOW.md`
- `.cursor/rules/04-cursor-workflow.mdc`
- `docs/architecture.md`
- `prompts/system/CORE_PROMPT.md`

**Objective**
Implement the core of AgentFactory in agent_factory.py (lazy jinja2 env from templates sibling, full create_specialist with name validation using regex, render using the .j2 , write the .py with prominent AUTO-GENERATED header, use SpecialistStorage(category) to init the two json files, build RegisteredAgent entry + registry.register_agent, force a get_agent_fn to exercise the load path for current process, if auto_commit: _commit_artifacts, return rich summary dict). Implement _commit_artifacts (subprocess git with clear msg) and _refine_with_llm stub. Create tests/test_agent_factory.py with the smoke tests from plan (tmp dirs + auto_commit=False, assert files on disk with header, fn loadable, second create no-op, refine stub test with mock). Strictly enforce the user's notes on auto_commit and header.

This slice delivers the factory that can create (in test mode). Real git only outside tests.

**Lightweight priority (from approved detailed plan + user approval notes - obey strictly)**
"Keep every slice small, explicit, and easily reviewable. Prioritize getting basic creation + registration + dynamic loading + routing working cleanly before any polish (LLM refine, advanced storage logic, etc.). Strictly enforce auto_commit=False + temporary paths in all tests. Real git commits only happen on actual (non-test) runs. Generated agents must always include the prominent AUTO-GENERATED header and be committed to git on real usage."

**Extra Guidance from Paul**
Prioritize simplicity. If a slice feels complex, simplify the implementation and document it. Generated code must always have the full AUTO-GENERATED header. Tests must never trigger real git commits.

For this slice: core creation path + tests with auto_commit=False + tmp; refine is stub; header always present in generated; no full LLM refine body (stub + test only).

**Constraints & Principles**
- Strictly limited scope (see box below).
- The .j2 and create logic must match the approved plan's design exactly (copy the full jinja template, the create_specialist contract, the _commit and refine notes).
- Small, reviewable change only.
- Smoke -q default.
- This is slice 04 of the small sequence.

**Context**
- Slices 01-03 have scaffold, registry, base.
- The approved plan has the exact jinja template (with AUTO-GENERATED, get_specialist_storage, {{ agent_name }}, specialist= kwarg, etc.), the create_specialist logic sketch, _commit_artifacts, and the test_agent_factory.py tests (with env tmp, auto_commit=False, assert header "AUTO-GENERATED by Agent Factory", etc.).
- Current patterns to reuse: SpecialistStorage from 03, registry from 02, atomic from classification, subprocess for git (new but simple).
- Later slices will wire the factory (05).

See approved plan "Jinja2 Templates", "Agent Factory Implementation Notes", "Risk / Mitigation", and Step 4 verification.

**Exact Steps (perform in order)**
1. **Claim the task first (mandatory per WORKFLOW.md)**: Scan `prompts/cursor/next/`, select the oldest (this one), **immediately move** it to `prompts/cursor/in-progress/2026-06-07-agent-factory-04-agent-factory/prompt.md` (or the exact name). Only then begin work. Document the move in your output.md. Never work on a file still in next/.

2. **Discovery (read-only)**: 
   - Read the approved plan sections listed above. Read the current factory/agent_factory.py stub and the .j2 placeholder from slice 01, and the base from 03.
   - Run `git status`, `uv run pytest -m smoke -q`, `ls src/agents/factory/ src/agents/specialists/`.
   - Confirm jinja2 is installed from slice 01.

3. **Fill `src/agents/factory/agent_factory.py`** (matching the approved design exactly):
   - Add imports (jinja2, from pathlib import Path, from typing import Any, datetime, re, subprocess, os, sys; relative from ..registry import get_agent_registry, RegisteredAgent; from ..specialists.base import SpecialistStorage).
   - _default_specialists_dir() using os.getenv("MYCELIUM_SPECIALISTS_DIR", "src/agents/specialists")
   - class AgentFactory: __init__ (self, registry=None, specialists_dir=None): self.registry = registry or get... ; self.specialists_dir = ... ; self.env = jinja2.Environment(loader=jinja2.FileSystemLoader( Path(__file__).parent / "templates"), trim_blocks=True, lstrip_blocks=True)
   - create_specialist(self, category: str, agent_name: str, description: str, examples: list[str] | None = None, *, llm_refine: bool = False, auto_commit: bool = True) -> dict[str, Any]:
     - validate: if not re.match(r'^[a-z][a-z0-9_]*_specialist$', agent_name): raise ValueError...
     - if self.registry.has_agent(agent_name): return {"created": False, "reason": "already registered"}
     - template = self.env.get_template("specialist_agent.py.j2")
     - now = datetime.now(timezone.utc).isoformat()
     - raw_code = template.render(agent_name=agent_name, category=category, description=description, examples=examples or [], created_at=now)
     - code = raw_code
     - if llm_refine: code = self._refine_with_llm(raw_code, agent_name)
     - self.specialists_dir.mkdir(parents=True, exist_ok=True)
     - py_path = self.specialists_dir / f"{agent_name}.py"
     - py_path.write_text(code, encoding="utf-8")
     - SpecialistStorage(category=category)  # side effect ensures data/agents/<cat>/
     - entry = RegisteredAgent( name=agent_name, category=category, description=description, module_path=f"agents.specialists.{agent_name}", entrypoint=agent_name, storage_path=f"data/agents/{category}/storage.json", strategy_path=f"data/agents/{category}/storage_strategy.json", is_generated=True, created_at=now )
     - self.registry.register_agent(entry)
     - fn = self.registry.get_agent_fn(agent_name)  # exercises load
     - committed = False
     - if auto_commit and "pytest" not in sys.modules and os.getenv("MYCELIUM_FACTORY_AUTO_COMMIT", "1") == "1":
       committed = self._commit_artifacts(py_path, agent_name, category)
     - return {"created": True, "agent_name": agent_name, "path": str(py_path), "committed": committed, ...}
   - _refine_with_llm(self, code: str, agent_name: str) -> str:  # stub for now, documented
     # TODO slice 07: lazy ChatOpenAI, prompt for review/style match with core_data.py, temp=0, guard contains def agent_name, return improved or code
     return code
   - _commit_artifacts(self, py_path: Path, agent_name: str, category: str) -> bool:
     # find repo root by walking for .git
     # rel = ...
     # msg = f"feat(agents): auto-generate {agent_name} for category '{category}'"
     # subprocess.run(["git", "add", str(rel), str(registry_path), str(data/agents/cat/* ) ], cwd=root, check=True)
     # subprocess.run(["git", "commit", "-m", msg], cwd=root, check=True)
     # return True
     # except: log, return False
   - get_agent_factory / reset_agent_factory singletons (similar pattern; reset can reset registry too if wanted).

4. **Ensure/Update the template `src/agents/factory/templates/specialist_agent.py.j2`** to the full exact from the plan's "Jinja2 Templates" design section (the one with AUTO-GENERATED header, get_specialist_storage, _coerce, _resolve..., {{ agent_name }}(state) that uses SpecialistStorage, classifications for my_attrs, response_non_core with specialist= kwarg, payload with classifications, etc.).

5. **Create `tests/test_agent_factory.py`** (new file) with several @pytest.mark.smoke tests from the plan Step 4:
   - Use monkeypatch/env for tmp registry + tmp specialists dir.
   - create(..., auto_commit=False)
   - assert py written, "AUTO-GENERATED by Agent Factory" in header, category in it, SpecialistStorage used (jsons in tmp), registry has entry, fn = get_agent_fn(name) is callable and when called with stub MyceliumGraphState returns dict with "response" etc.
   - second create same name is no-op.
   - Test for refine path with fake llm (class _FakeLLM that returns improved code str containing a marker; assert create with llm_refine=True used it).
   - All tests enforce no real commit / no src pollution.

6. **Verification (smoke + manual for this slice)**:
   - `uv run pytest -m smoke -q -k "factory or agent_registry"`
   - Manual from approved Step 4 (with envs, auto_commit=False):
     `MYCELIUM_AGENT_REGISTRY_PATH=/tmp/r.json MYCELIUM_SPECIALISTS_DIR=/tmp/s uv run python -c '
     from agents.factory import get_agent_factory, reset_agent_factory
     from agents.registry import reset_agent_registry
     reset...
     f = get_agent_factory()
     info = f.create_specialist("contact", "contact_specialist", "Direct contact info", auto_commit=False)
     print(info)
     print((Path("/tmp/s") / "contact_specialist.py").read_text()[:300])
     '`
   - `git status` (no src/agents/specialists/ or data/ pollution from the test; only test file if any).
   - `uv run ruff check src/agents/factory src/agents/specialists/base.py tests/test_agent_factory.py`
   - Confirm existing smoke still green.

7. **Output artifacts (exactly per WORKFLOW)**:
   - Create `prompts/cursor/done/2026-06-07-agent-factory-04-agent-factory/output.md` with summary ( "followed lightweight: auto_commit=False + tmp in all tests; header always; jinja and create from plan exact; real git only in non-test"; decisions), git diff --stat + key contents (the header in example generated), full verif outputs, **explicit note + diff stat confirming test file limited per Guard rule**, scope confirmation (only factory py + .j2 + new test), open Qs.
   - Move/copy this prompt into done/ as prompt.md.
   - Remove only the claimed from in-progress/.

8. **Process hygiene**:
   - Follow claiming exactly.
   - Stop on scope (do not wire to supervisor/graph yet, no real git commits in this slice's tests, no mcp, no full responses etc.).
   - Do not touch approved plan.

## Scope Boundaries (Strict)
You may only create or modify files under the following paths:
- `src/agents/factory/agent_factory.py`
- `src/agents/factory/templates/specialist_agent.py.j2` (if filling the full)
- `tests/test_agent_factory.py` (new — strictly the tests listed in Exact Steps + Guard rule above; no extra)

**Out of Scope (Do Not Touch)**
- src/agents/dispatch.py, supervisor.py, graphs/core.py, models/state.py, responses.py, core_data.py, registry.py (beyond using), specialists (beyond using base), mcp, test_core_graph.py, conftest.py, docs/, any real data/agents/ or src/agents/specialists/*.py in source, git commits from tests, the plan files, TODO.md, etc.
- Do not implement the real _refine body (stub + test only; full in 07).
- Do not run any command that would cause real git commit of generated agents in this slice.

If you determine that changes outside this scope are necessary:
- **Stop immediately.**
- Clearly document the problem in your `output.md`.
- Do **not** make the out-of-scope changes.
- Create a follow-up prompt in `prompts/cursor/next/` describing what needs to be done instead.

This rule is mandatory.

## Test Execution Policy
- Default: `uv run pytest -m smoke -q` only.
- All factory tests must strictly use auto_commit=False + tmp MYCELIUM_*_PATH / SPECIALISTS_DIR (enforce the pytest guard and user's note).
- The refine test uses mock llm, no key/network.
- No full tests in this slice.

**Guard against excessive insertions (even in new dedicated test files):**
Keep `tests/test_agent_factory.py` strictly to the smoke tests *specified in this slice's Exact Steps* (tmp + auto_commit=False, header check, loadable fn, second create no-op, refine stub with mock). Do not add extra tests, helpers, or coverage beyond what's listed. If you feel more tests are needed, document in output.md and propose a follow-up prompt. This keeps the slice small.

## Required Output Location & Artifacts
- `prompts/cursor/done/2026-06-07-agent-factory-04-agent-factory/output.md`
- The claimed prompt moved to the done/ subdir as `prompt.md`

Follow the claiming process in `prompts/cursor/WORKFLOW.md` exactly before doing any implementation work.

## Suggested Acceptance (for Grok + Paul review)
After Cursor delivers, we will review the output.md, confirm only the listed files were touched, **confirm test file stayed minimal per Guard rule + diff stat**, re-run smoke + the manual /tmp create (inspect header has "AUTO-GENERATED by Agent Factory" and category, jsons created in /tmp/agents/contact/, registry updated, fn callable and works with stub state, second create no-op, refine test passes), confirm git status shows no pollution or real commits from tests, confirm header always present, scope exact, add review.md if needed, then (if good) commit this small slice and prepare the next (05-dispatch-graph-supervisor).

Start by claiming the file (move to in-progress/). Good luck — make the change small, explicit, and reviewable. Reference the approved plan for every detail (exact jinja, create contract, test expectations, auto_commit enforcement).
