# agent-factory-04-agent-factory — Output

## Claim

Moved `prompts/cursor/next/2026-06-07-agent-factory-04-agent-factory.md` → `in-progress/.../prompt.md` before implementation.

## Summary

Implemented **AgentFactory** (slice 04):

- Lazy Jinja2 `Environment` over `factory/templates/`
- Full **`specialist_agent.py.j2`** per approved plan (AUTO-GENERATED header, `SpecialistStorage`, `{{ agent_name }}` node)
- **`create_specialist`**: name validation (`^[a-z][a-z0-9_]*_specialist$`), render, write `.py`, init storage via `SpecialistStorage`, `register_agent`, `get_agent_fn` load check
- **`auto_commit`**: skipped when `pytest` in `sys.modules` or `MYCELIUM_FACTORY_AUTO_COMMIT != "1"`
- **`_commit_artifacts`**: `git add` py + registry + `data/agents/<cat>/*.json`, commit with `feat(agents): auto-generate ...`
- **`_refine_with_llm`**: stub (slice 07)
- Singletons `get_agent_factory` / `reset_agent_factory`

New **`tests/test_agent_factory.py`** (3 smoke tests only, Guard rule):

1. Create with tmp envs → header, registry, storage jsons, fn callable (not_found invoke path)
2. Second create → no-op
3. `llm_refine=True` with mocked refine marker

## Test file (Guard rule)

New file only — `tests/test_agent_factory.py` (~130 lines, 3 tests). No changes to shared test files.

```
git diff --stat tests/test_agent_factory.py
(new file — 3 smoke tests)
```

## Note: `specialist=` kwarg

Template calls `response_non_core(..., specialist=...)` per plan. **`responses.py` is out of scope** for this slice (slice 06). Factory tests invoke the generated fn on the **not_found** path only (no `specialist` kwarg). Match + research narrative path works after slice 06 adds `specialist` to `response_non_core`.

## Verification

### Smoke (factory + registry)

```
$ uv run pytest -m smoke -q -k "factory or agent_registry"
.....                                                                    [100%]
5 passed, 30 deselected
```

### Full smoke

```
26 passed, 9 deselected
```

### Ruff

```
All checks passed!
```

### Manual (/tmp, auto_commit=False)

```
{'created': True, 'agent_name': 'contact_specialist', ..., 'committed': False, 'fn_loaded': True, ...}
"""contact_specialist — auto-generated specialist agent for category "contact".

AUTO-GENERATED by Agent Factory on ...
```

No `src/agents/specialists/*.py` or `data/agents/` pollution from tests.

## Scope

Only modified/created:
- `src/agents/factory/agent_factory.py`
- `src/agents/factory/templates/specialist_agent.py.j2`
- `tests/test_agent_factory.py` (new)

## Ready for slice 05

`2026-06-07-agent-factory-05-dispatch-graph-supervisor.md` — dispatch, graph wiring, supervisor creation trigger.
