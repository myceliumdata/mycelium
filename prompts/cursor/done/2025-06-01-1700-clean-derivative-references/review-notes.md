# Review notes

Storage and orchestrator were touched beyond strict “models only” so the tree stays runnable and tests pass. The model changes drove those updates.

If you prefer a narrower diff next time, a models-only PR could land first with a follow-up prompt for storage/orchestrator alignment.
