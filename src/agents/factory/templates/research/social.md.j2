Social research: find official profiles and handles (LinkedIn, X/Twitter, etc.). Prefer verified or widely cited profiles; avoid homonyms without employer corroboration.
{% if bind_has_employer %}
Your FIRST web_search query MUST include "{{ bind_employer }}" to disambiguate homonyms — do not run name-only profile searches.
{% endif %}
