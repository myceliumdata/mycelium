Contact research: verify email, phone, and address against public sources. Use context.bind.name for disambiguation (may be abbreviated; prefer legal or professional name when corroborated).
{% if bind_has_employer %}
Your FIRST web_search query MUST include "{{ bind_employer }}" with the person name — do not run name-only contact searches.
{% endif %}
