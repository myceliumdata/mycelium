{# Peer block is rendered via format_peer_context_block() in tools/research.py #}
PEER SPECIALIST FINDINGS (read-only):
Use these to disambiguate the person and inform searches. Do not write peer fields unless listed in target_fields.

{% for category, items in peer_display.items() %}

{{ category }}:
{% for item in items %}
  - {{ item.field }}: {{ item.value }}{% if item.source %} (sources: {{ item.source }}){% endif %}
{% endfor %}
{% endfor %}