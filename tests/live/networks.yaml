baseball:
  catalog: catalogs/baseball.yaml
  anchors: anchors/baseball_aaron_lahman_v2025.json
  default_root: ~/mycelium-networks/baseball
  phases: [preflight, identity, m2, pitching, team_season, fielding, roster, franchise, derive, bio_research, infra]
  refresh_before_gate: true

crm-seeded:
  catalog: catalogs/crm_seeded.yaml
  anchors: anchors/crm_seeded_v1.json
  default_root: ~/mycelium-networks/crm-seeded
  phases: [preflight, protocol, research, negative]
  refresh_before_gate: true

crm-metering:
  catalog: catalogs/crm_metering.yaml
  anchors: anchors/crm_seeded_v1.json
  default_root: ~/mycelium-networks/crm-metering
  phases: [preflight, metering]
  refresh_before_gate: true

crm-empty:
  catalog: catalogs/crm_empty.yaml
  anchors: null
  default_root: ~/mycelium-networks/crm-empty
  phases: [preflight, growth]
  refresh_before_gate: true
