{
  "name": "crm-metering",
  "display_name": "CRM metering demo",
  "description": "CRM example with metering.enabled for hands-on quote_required negotiation demos.",
  "bootstrap": {
    "module": "network.bootstrap.handlers.default_seed",
    "handler": "DefaultSeedHandler"
  },
  "mvr": {
    "default_record_type": "person",
    "record_types": {
      "person": {
        "bind_fields": ["name", "employer"],
        "description": "CRM people: display name plus current employer before bind and research.",
        "new_records": "query_allowed"
      }
    }
  },
  "metering": {
    "enabled": true,
    "description": "Negotiation demo: quote_required before attribute research. Payment disabled — quote_id accepts without pay_quote.",
    "default_funding_model": "marginal",
    "meter_first_delivery": true,
    "quote_provider": "builtin",
    "principal": {
      "marginal_optional": true,
      "required_for_funding_models": ["sponsor_public", "pool"]
    },
    "payment": {
      "enabled": false,
      "provider": "mock",
      "require_paid_before_accept": true
    }
  }
}
